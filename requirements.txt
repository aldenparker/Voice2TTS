# Core pipeline
faster-whisper==1.2.1        # STT (CTranslate2 backend)
piper-tts==1.7.0             # TTS
onnxruntime==1.28.0          # Silero VAD + Piper
onnx==1.20.0                 # Voice Studio only: reads and rewrites the speaker
                             # table when baking a designed voice. onnxruntime
                             # cannot substitute -- it drops unused initializers,
                             # so the speaker embeddings are not readable from a
                             # loaded session.
sentencepiece==0.2.2         # tokenizer for the translation models. The
                             # models themselves are converted at build time by
                             # scripts/convert_mt.py; nothing here needs torch.
sounddevice==0.5.5           # PortAudio bindings
soxr==1.1.0                  # streaming resampler
numpy>=2.0

# GPU runtime for CTranslate2. See voice2tts/cuda.py -- these DLLs need preloading
# by absolute path on Windows. Drop both lines for a CPU-only install.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12>=9,<10

# UI and input
pystray==0.19.5
pillow>=10
pynput==1.8.2

# Config
tomli-w>=1.0
